# Shared requirements are defined in the repo root: /requirements.txt
# Both ml/Dockerfile and backend/Dockerfile copy from the root and install it.
# Edit /requirements.txt to add or change dependencies.
#
# For local dev (outside Docker), run from repo root:
#   pip install -r requirements.txt

